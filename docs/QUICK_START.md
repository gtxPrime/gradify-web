# ⚡ Quick Start: Gradify Web

Get Gradify Web running locally in under 2 minutes!

## 🏃‍♂️ Fast Track

1. **Clone & Enter**

   ```bash
   git clone https://github.com/gtxPrime/gradify-web.git
   cd gradify-web
   ```

2. **Serve** (using Python 3 as a quick fallback if you don't have Node)

   ```bash
   python -m http.server 8000
   ```

   _Or use VS Code Live Server extension for auto-reload._

3. **View**
   Open `http://localhost:8000` in your web browser.

---

## 💡 Navigating the Code

- To change the UI, look inside `*.html` and `css/`.
- To modify functionality, tweak the files in `js/`.
- Images and graphics are situated in `assets/`.

Need more detail? Head over to the [Build & Run](BUILD_AND_RUN.md) guide!
